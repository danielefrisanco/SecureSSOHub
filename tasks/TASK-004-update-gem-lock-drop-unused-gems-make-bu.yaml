id: TASK-004
title: Update gem lock, drop unused gems, make bundle install succeed
type: chore
status: done
priority: critical
created: '2026-09-18'
updated: '2026-09-18'
branch: task/004-update-gem-lock-drop-unused-gems-make-bu
depends_on: []
context: 'TODO T03 (docs/AUDIT.md §3, §5.5). Gemfile.lock pins jwt_auth_client 0.1.0, rack-jwt-verifier

  0.1.0, header_guard 0.1.1 while 0.2.0 / 0.3.0 / 0.3.1 are published and are what the code needs.

  omniauth_syncer is a client-side gem the hub never uses. omniauth-ssoprovider is only needed as the

  reference client in the test suite (and later the developer page), so move it (and omniauth-oauth2)

  to the development/test group. Keep omniauth + omniauth-rails_csrf_protection only if still

  required after T04 — if unsure, keep them and note it. jwt_auth_client 0.2.0 has breaking config

  changes (validated configure block, issuer required, 32-byte key) — the model/initializer fix is

  T06, so after this task the app may still not boot; that is expected and must be noted.'
acceptance_criteria:
- Gemfile.lock resolves jwt_auth_client 0.2.0, rack-jwt-verifier 0.3.0, header_guard 0.3.1
- omniauth_syncer is removed from Gemfile and Gemfile.lock
- omniauth-ssoprovider and omniauth-oauth2 are in the development/test group
- '`bundle install` succeeds locally and `BUNDLE_DEPLOYMENT=1 bundle install` succeeds (lock in sync with
  Gemfile)'
- A task note states whether the app boots after this change and which follow-up task fixes it if not
plan: []
notes:
- at: 2026-09-18 15:19
  text: started on task/004-update-gem-lock-drop-unused-gems-make-bu
- at: 2026-09-18 15:21
  text: 'DONE: Gemfile/Gemfile.lock updated. Verified: lock resolves jwt_auth_client 0.2.0, rack-jwt-verifier
    0.3.0, header_guard 0.3.1; omniauth_syncer absent from both files; omniauth-oauth2 + omniauth-ssoprovider
    in group :development, :test; bundle install OK; BUNDLE_DEPLOYMENT=1 bundle install (into a scratch
    BUNDLE_PATH) exit 0. Boot: the app does NOT boot yet - RAILS_ENV=test rails runner and rspec both
    stop at the pre-existing LoadError ''Could not find matching strategy for :ssoprovider'' from config/initializers/omniauth_ssoprovider.rb:9
    (same as before this task); TASK-005 removes that initializer. With omniauth-ssoprovider now dev/test-only
    that initializer would also NameError in production until TASK-005 lands - no regression, production
    did not boot either. omniauth + omniauth-rails_csrf_protection kept (still loaded by the initializer
    until TASK-005 decides). Test gate: rspec cannot run until TASK-005.'
- at: 2026-09-18 15:30
  text: 'Test gate waived by the user on 2026-09-18: bundle exec rspec fails at boot with the pre-existing
    LoadError from config/initializers/omniauth_ssoprovider.rb:9 (removed by TASK-005); no application
    code changed on this branch.'
- at: 2026-09-18 15:32
  text: done — reviewer PASS; verified by bundle check, frozen deployment install into scratch path, lock/Gemfile
    inspection; test gate waived (boot LoadError fixed by TASK-005).
