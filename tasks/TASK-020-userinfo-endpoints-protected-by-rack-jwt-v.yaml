id: TASK-020
title: Userinfo endpoints protected by rack-jwt-verifier
type: feat
status: done
priority: critical
created: '2026-09-18'
updated: '2026-09-18'
branch: task/020-userinfo-endpoints-protected-by-rack-jwt
depends_on:
- TASK-019
context: "TODO T17 (audit §3.2, §3.3, §5.4). Two userinfo endpoints:\n- `GET /oauth/userinfo` — standard\
  \ OIDC, provided by doorkeeper-openid_connect (claims per scope).\n- `GET /api/v1/userinfo` — the contract\
  \ `omniauth-ssoprovider` 0.1.2 hard-codes\n  (`raw_info['id']` → uid, `name`, `email`). Served by the\
  \ hub's own `Api::V1::UserinfoController`\n  behind rack-jwt-verifier, returning `{id: sso_id, sub:\
  \ sso_id, name, email, email_verified,\n  roles: [\"admin\"] or []}` with `name`/`email` gated by the\
  \ `profile`/`email` scopes (id/sub/roles\n  always). This is also the first hub API endpoint, so it\
  \ sets the pattern for `/api/**`.\n\nrack-jwt-verifier on `/api/**` (own gem, 0.3.x) — mount in config/application.rb\
  \ or an\ninitializer scoped by `skip:` to everything except `/api/`:\n- key source: the hub verifies\
  \ its OWN tokens, so use `public_key:` from SigningKey (in-process,\n  no self-HTTP) — but rotation\
  \ means two keys; if the gem's `public_key` cannot take a key set,\n  implement a tiny in-process JWKS\
  \ source (the gem's `jwks_url` + `cache_store` pointing at a\n  Rack::Test-served URL is NOT acceptable\
  \ in production). Record the limitation as a gem\n  follow-up in TODO.md (`[gem: rack-jwt-verifier]`:\
  \ accept a callable/in-process JWKS provider).\n- `decode_options: { iss: HUB_ISSUER, aud: OAuth::Resources.hub_api\
  \ }` — tokens minted for another\n  audience (a client's own aud, the MCP resource) are refused with\
  \ 401 invalid_token.\n- `require_token: true`, `json_errors: true`, `algorithms: [\"RS256\"]`, leeway\
  \ 30.\n- replay_cache is left off until Redis (T23); note it.\nThe controller reads `env[\"rack_jwt_verifier.payload\"\
  ]`, loads the user by `sub` (sso_id),\nrefuses disabled users (401, `invalid_token`) and revoked tokens\
  \ (check Doorkeeper token by jti\nvia OAuth::Tokens — JWTs are self-contained, so revocation must be\
  \ checked explicitly here; keep\nit one indexed query).\n\nSpecs (spec/requests/api/v1/userinfo_spec.rb):\
  \ 401 with WWW-Authenticate Bearer when no token;\n401 for wrong aud, wrong iss, expired, tampered signature;\
  \ 200 with exact JSON for a token with\nopenid+profile+email; name/email absent without those scopes;\
  \ roles [\"admin\"] for admins; 401 for\ndisabled user and for a revoked token; `/oauth/userinfo` returns\
  \ OIDC claims for the same token.\nPlus an interop spec that `RackJwtVerifier::Middleware` in a standalone\
  \ Rack app configured with\nthe hub's JWKS document (public_key from SigningKey, and jwks mode via WebMock\
  \ `to_rack`) accepts\na hub token and exposes the payload — this is the proof that downstream services\
  \ can use the gem\nunchanged.\n"
acceptance_criteria:
- GET /api/v1/userinfo returns {id, sub, name, email, email_verified, roles} with scope gating, exactly
  what omniauth-ssoprovider expects, and GET /oauth/userinfo returns OIDC claims
- /api/** is protected by rack-jwt-verifier (in-process key source, iss + hub-API aud required, RS256
  only); tokens for other audiences, tampered/expired tokens and missing tokens get 401 with the RFC 6750
  challenge; disabled users and revoked tokens get 401
- 'Interop spec proves a standalone Rack app with RackJwtVerifier::Middleware (JWKS mode) accepts the
  hub''s access token; any gem limitation found is recorded in TODO.md as [gem: rack-jwt-verifier]'
- Doorkeeper isolation spec passes; rspec/rubocop/brakeman pass
plan:
- Mount rack-jwt-verifier with the in-process key source; write the 401 specs first.
- Api::V1::UserinfoController + revocation/disabled checks + specs.
- Interop spec with the gem in JWKS mode; record limitations.
- Suite; closing note.
notes:
- at: 2026-09-18 18:11
  text: 'alignment (after TASK-016/017): aud for the hub API is OAuth::Resources.hub_api (issuer + /api),
    issuer from Rails.configuration.x.oauth.issuer; disabled-user check pattern already used by OAuth::TokenRules
    (User.exists?(id:, disabled_at: nil)); revoked-token lookup by jti depends on TASK-019''s jti claim
    (OAuth::Tokens::Token#jti is currently the raw token string — align it). Doorkeeper isolation spec
    allows Doorkeeper:: only under app/services/oauth, config/initializers and config/routes.rb — the
    API controller must go through OAuth::Tokens.'
- at: 2026-09-18 19:44
  text: started on task/020-userinfo-endpoints-protected-by-rack-jwt
- at: 2026-09-18 19:58
  text: 'closing: DONE. Files: db/migrate/20260918210000_add_jti_to_oauth_access_tokens.rb (+schema),
    app/services/oauth/token_record.rb (prepended into Doorkeeper::AccessToken: jti chosen on the row
    before the JWT is generated, passed to the generator), token_payload.rb (jti from attributes), tokens.rb
    (active?(jti:) one indexed query; Token#jti now the claim), config/initializers/rack_jwt_verifier.rb,
    app/controllers/api/base_controller.rb (ActionController::API; revoked -> 401 invalid_token, disabled
    -> 401 invalid_token, RFC 6750 challenge), app/controllers/api/v1/userinfo_controller.rb, routes,
    spec/support/hub_access_token.rb, spec/requests/api/v1/userinfo_spec.rb (19), spec/requests/api/rack_jwt_verifier_interop_spec.rb
    (4), TODO.md (T48 confirmed + new T52), docs/ARCHITECTURE.md par. Commits 4ad8fb7, 46b47a2, 2b6e270,
    7fc521a. rspec 240/0, rubocop, brakeman clean. Verification: criterion 1 by userinfo_spec (exact JSON,
    scope gating, roles) and the /oauth/userinfo examples (Doorkeeper finds the JWT by its hash, no change
    needed); criterion 2 by the 401 examples (missing token -> bare Bearer, wrong aud incl. id_token,
    wrong iss, expired, tampered, foreign key, revoked, unissued jti, disabled) plus guard-reach examples
    and a previous-key rotation example; criterion 3 by the interop spec (gem as shipped, JWKS mode over
    WebMock to_rack against the real stack, and public_key mode); criterion 4 by the suite. Decisions
    to confirm: (a) gem limitation: public_key takes one key and jwks_url always fetches, so the initializer
    passes a placeholder public_key plus the hub JWKS as the ruby-jwt jwks decode option (documented pass-through,
    resolved per request; TODO T48); (b) under SECRET_KEY_BASE_DUMMY a throwaway key keeps the guard mounted;
    (c) replay_cache off until Redis (T23); (d) OAuth::Tokens.active? checks existence + revoked_at only,
    expiry is the JWT exp; tokens issued before the jti column are treated as revoked (10-minute lifetime);
    (e) aud string is built in the initializer from config.x (Resources cannot be autoloaded there). Finding
    for the gem: an id_token passes rack-jwt-verifier at a downstream service whose aud is its client_id
    (same iss/key/aud) — only typ tells them apart; recorded as T52 [gem: rack-jwt-verifier]. WebMock
    to_rack seeds rack.session with a Hash that rack-session 2 cannot commit; the interop spec strips
    it in a one-line adapter.'
- at: 2026-09-18 20:07
  text: 'review fix (82e388a): reviewer FAIL on a guard bypass — GET //api/v1/userinfo skipped the middleware
    because the skip lambda matched raw PATH_INFO while Journey squeezes repeated slashes. Fixed by normalising
    with ActionDispatch::Journey::Router::Utils.normalize_path, Api::BaseController now answers 401 when
    no verified payload is present, regression specs for //api, ///api and trailing slash. While reproducing,
    found an order-dependent failure: Devise configures Warden (failure app, intercept_401=false) only
    when routes are finalised, lazily in test/dev, so a first-request 401 from the guard crashed in Warden
    (''No Failure App provided''). The guard now sits before Warden::Manager (insert_before) and a spec
    pins the order. Production unaffected (eager_load finalises routes at boot). rspec 243/0.'
- at: 2026-09-18 20:10
  text: 'review: FAIL (//api bypass) then PASS after 82e388a. rspec 243/0, rubocop, brakeman clean. Decisions
    for the user: jwks passed as a ruby-jwt decode option next to a placeholder public_key (TODO T48);
    replay_cache off until Redis (T23); tokens issued before the jti column count as revoked; T52 records
    that an id_token would pass rack-jwt-verifier at a downstream service using aud = client_id (gem needs
    a typ at+jwt check). Gem prompts inputs: TODO.md T48/T52, spec/requests/api/rack_jwt_verifier_interop_spec.rb,
    docs/ARCHITECTURE.md section 3.'
- at: 2026-09-18 20:10
  text: 'handoff: DONE: TASK-018, TASK-019, TASK-020 reviewed PASS and merged into develop (7ad348d),
    plus fix(docker) 57a0d4d for the image build (HUB_ISSUER placeholder at precompile). rspec 243/0,
    rubocop, brakeman clean. | NEXT: user pauses to prepare gem prompts with the Fable model: inputs are
    TODO.md T48 (rack-jwt-verifier in-process key set) and T52 (typ at+jwt check), spec/requests/api/rack_jwt_verifier_interop_spec.rb,
    docs/ARCHITECTURE.md section 3 (token payload) and section 4. Then resume Phase 1 with /harness:run-task
    TASK-021 (start-task steps first), then 022 -> 023/024/025; stop before the TASK-026 gate. Local env:
    POSTGRES_HOST=localhost for rspec; bin/rails no longer needs JWT_SERVICE_SECRET. | BLOCKERS/QUESTIONS:
    none. User merges develop -> main and pushes; CI has still never run on GitHub.'
